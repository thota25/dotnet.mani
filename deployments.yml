apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp-deployment
  labels:
    app: myapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: myapp
  template:
    metadata:
      labels:
        app: myappapp
    spec:
      containers:
        - name: myapp-deployment
          image: d1464108bc764740b25896e992f62a26/dotnetmani-app:latest
          ports:
            - containerPort: 8080