apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: dotnetmani-app-hpa
  namespace: manish
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: dotnetmani-app-deployment
  minReplicas: 1
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 50